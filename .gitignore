# secrets — never commit API keys
.env
.env.*
.dev.vars
*.key

# macOS
.DS_Store

# test / scratch output
dist/
*.optimized.*
*-min.png
*@2x.png

# SDD workspace scratch (superpowers)
.superpowers/

# process artifacts — specs & plans stay local, not in the public repo
docs/specs/
docs/plans/
